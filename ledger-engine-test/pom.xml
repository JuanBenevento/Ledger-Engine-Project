<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.juanbenevento</groupId>
        <artifactId>ledger-engine</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>ledger-engine-test</artifactId>
    <name>ledger-engine-test</name>
    <description>Shared test utilities, Testcontainers configuration</description>

    <dependencies>
        <!-- Internal modules for testing -->
        <dependency>
            <groupId>com.juanbenevento</groupId>
            <artifactId>ledger-engine-infrastructure</artifactId>
        </dependency>
        <dependency>
            <groupId>com.juanbenevento</groupId>
            <artifactId>ledger-engine-api</artifactId>
        </dependency>
        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Exclude Docker-dependent integration tests from default unit test run -->
                    <excludes>
                        <exclude>**/AccountHistoryE2ETest.java</exclude>
                        <exclude>**/LedgerEngineCoreApplicationTests.java</exclude>
                        <exclude>**/AccountLifecycleIT.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
