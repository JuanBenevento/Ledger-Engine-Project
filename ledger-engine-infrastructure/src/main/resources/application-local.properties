# Local development profile - disables OAuth2 Resource Server for Swagger UI access
ledger.security.oauth2.enabled=false

# Exclude OAuth2 Resource Server auto-configuration entirely in local dev
spring.autoconfigure.exclude=org.springframework.boot.security.oauth2.server.resource.autoconfigure.servlet.OAuth2ResourceServerAutoConfiguration
